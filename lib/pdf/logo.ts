import path from 'path'

/** Logo Sharks em azul marinho (#10203f) para PDF. */
export function caminhoLogoPdfMarinho(): string {
  return path.join(process.cwd(), 'public/sharks-logo-marinho.svg')
}
